"""Recover AI -- diagnose failed Razorpay payments, drive
compliant bounded recovery, and account for every decision.

Public entry points: `recover_ai.app.build_pipeline` (wire a
Pipeline from Settings) and the `recover-ai` CLI.
"""

__version__ = "1.0.0"
