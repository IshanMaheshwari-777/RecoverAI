"""Revenue Recovery Agent -- diagnose failed Razorpay payments, drive
compliant bounded recovery, and account for every decision.

Public entry points: `revenue_recovery.app.build_pipeline` (wire a
Pipeline from Settings) and the `revenue-recovery` CLI.
"""

__version__ = "1.0.0"
